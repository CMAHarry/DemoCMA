$kind: http-example
name: '200 OK'
request:
  url: '{{baseUrl}}/api/products/bom-items/{{materialId}}'
  method: DELETE
response:
  statusCode: 200
  statusText: OK
  headers:
    - key: Content-Type
      value: application/json
  body:
    type: json
    content: |-
      {
        "success": true,
        "message": "BOM item deleted",
        "data": null
      }
